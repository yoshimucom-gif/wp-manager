# CHANGELOG / 引き継ぎメモ — Affiros リライト プラグイン

このファイルは、**別のチャット（セッション）で作業を引き継ぐため**の記録です。
新しいセッションでこのプラグインを触るときは、まず `README.md`（概要・使い方）と
このファイル（ファイル構成・改修履歴・設計判断・既知の制約）を読んでください。

---

## ファイル構成

```
affiros-rewrite/
├── affiros-rewrite.php   … 本体。定数定義・設定取得・メニュー登録・CSS読み込み
├── README.md             … 概要・インストール・設定・使い方
├── CHANGELOG.md          … このファイル（改修履歴・引き継ぎ）
├── admin/
│   ├── rewrite-page.php   … リライト実行画面（投稿一覧＋単記事/一括。JSはインライン）
│   ├── settings-page.php  … 設定画面（APIキー・モデル・リライト既定値）
│   └── ajax-handler.php   … AJAXエンドポイント（fetch_posts / run_single / save）
├── includes/
│   ├── claude-api.php     … Claude API ラッパー（自動リトライ付き）
│   ├── rewrite-engine.php … プロンプト生成・実行・出力パース・安全弁
│   ├── post-fetcher.php   … WP_Query での投稿取得・更新
│   ├── marker-inserter.php… 商品カードマーカー挿入（本体 insert_card_markers 準拠）
│   └── article-type.php   … 記事タイプ判定（本体 infer_title_article_type 準拠）
└── assets/
    └── admin.css         … 管理画面CSS
```

## 設計上の要点

- WP REST API を使わず `WP_Query` / `wp_update_post` で完結 → ホスティングの WAF・海外IP制限による 403 を回避する（これがプラグインの存在理由）。
- Claude APIキーは `wp-config.php` の定数 `AFFIROS_REWRITE_API_KEY` を最優先で使う。定義されていれば管理画面DBより優先され、プラグインの更新・再インストールでも消えない。
- 一括リライトは確認画面を経由せず順次保存する。ただし壊れた内容でライブ記事を上書きしないための安全弁あり（v0.4.0 参照）。
- リライト結果は `wp_update_post` 経由で保存 → WordPress のリビジョンで復元可能（リビジョン無効サイトでは画面に警告を出す）。

## バージョン履歴

### v0.4.3 (2026-05-22)
> 0.4.1 / 0.4.2 は欠番。本体リポジトリ外で作られた切り離しビルドが先にその番号を
> 使ってしまったため、正式な本体準拠ビルドを 0.4.3 とした（バージョン表記の一意化）。

**本体（wp_manager app.py）準拠の見直し**
- `marker-inserter.php` を本体 `insert_card_markers` / `DEFAULT_CARD_INSERTION_PATTERNS`
  の忠実移植に修正:
  - ranking のh3挿入を `after_each_h3_rank`（「第N位 / No.N / ①」等のランキング見出しのみ。
    0件時は最初のH2〜まとめH2間の全H3へフォールバック）に。従来は全h3に無差別挿入していた。
  - brand のマーカーを1個に（本体準拠。従来は余分な `before_matome_h2` があった）。
  - まとめH2の判定キーワードを本体と統一（まとめ / 総まとめ / 結論 / 要点）。
  - `top` / `bottom` 位置に対応、`repeat` を全位置で対応。
  - 挿入前に導入H2・早見表セクションを除去（本体の前処理を移植）。
- 記事タイプに「自動判定」を追加。本体 `infer_title_article_type` / `normalize_article_type`
  を PHP移植し、元記事タイトルから ranking / brand / column を判定する。
  種類の異なる記事を一括リライトしても各記事に正しいマーカー規則が当たる。
- リライトのタイプ別指示を本体 `build_article_type_prompt` 準拠に拡充。

**堅牢化**
- Claude API が一時的エラー（529 Overloaded / 429 / 5xx / 通信エラー）を返したとき、
  時間を置いて最大3回まで自動リトライ。一括リライト中の単発「Overloaded」失敗を低減。
- 一括リライト実行中にタブを閉じようとするとブラウザ警告を表示。
- 商品カードマーカーのチェックボックスは、記事タイプ未選択時は操作不能に。
- 一括リライトのログに、判定された記事タイプとマーカー挿入有無を表示。
- 目標文字数の指示を強化。設定した目標文字数を必ず目指し、許容下限を下回らないよう明示
  （「リライト＝短縮ではない」「不足分は実質的な情報で補う」を明記）。
  設定値より大幅に短く出力される問題に対応。

新規ファイル: `includes/article-type.php`

### v0.4.0 (2026-05-22)
**削除**
- 品質プリセット機能（`quality-presets.php` / `presets-page.php` と関連コード）。未使用だったため削除し、リライトのオプションは「設定画面の既定値」＋「記事タイプ指定」に集約。

**安全性強化（一括リライトのデータ破損リスクを解消）**
- Claude の出力が想定フォーマット（`===TITLE===` / `===CONTENT===`）で返らない場合、`WP_Error` で中断し保存しない（前置き混入などの混濁テキストでライブ記事を上書きしない）。
- 出力が出力上限で途中切れした場合（`stop_reason=max_tokens`）も中断。
- 元記事が 30000 字を超える場合は中断（末尾を失ったまま上書きするのを防ぐ）。
- `max_tokens` を目標文字数から動的算出（固定値8000による長文の途中切れを解消）。
- リビジョンが無効なサイトでは、リライト画面に上書き前の警告を表示。

**修正**
- Haiku のモデルIDを正規の `claude-haiku-4-5-20251001` に修正（旧 `claude-haiku-4-5` 等は読み込み時に自動マイグレーション）。

**整理**
- 未使用の `get_post` AJAXエンドポイントを削除。
- 中身が空だった `admin.js` を削除（`wp_localize_script` は jQuery 経由に変更）。
- 設定画面の誤った説明文を修正。

### v0.3.2 (2026-05-22)
- 設定保存時にAPIキーが空文字で上書き消去されるバグを修正（パスワード欄は空でも `''` を送るため `??` で既存キーを拾えていなかった）。これにより一括リライトボタンが意図せず無効化される問題も解消。
- APIキーを `wp-config.php` 定数で設定できるよう対応。
- 一括操作バーに、APIキー未設定時の理由表示と設定画面リンクを追加。

### v0.3.1
- Claude モデルIDを wp_manager 本体と統一。旧モデルIDは読み込み時に自動マイグレーション。

### v0.3.0
- 品質プリセット機能（→ v0.4.0 で削除）。
- 商品カードマーカー挿入機能。

### v0.2.0
- 単記事リライト＋結果プレビュー（左右比較・編集可）、一括リライト。

### v0.1.0
- 設定画面、投稿一覧（WP_Query）、カテゴリー/状態/検索フィルタ、ページネーション。

## 既知の制約・今後の検討事項

- **広告挿入定義のカスタム非対応**: 本体の「広告挿入定義」UIでマーカー規則を変更しても、プラグインは本体のJSONを読めないため反映されない。プラグインは `DEFAULT_CARD_INSERTION_PATTERNS` 相当のみ対応。
- **長文記事のタイムアウト**: Claude の応答がホスティング（ConoHa 等）の PHP / プロキシ制限を超えると「通信エラー」で失敗しうる。一括実行では Haiku・少数バッチが安全。
- **元記事の上限**: 30000 字。超える記事はリライトを中断する。
- **画面JS**: `rewrite-page.php` にインライン（約260行）。必要なら `admin.js` へ分離可能。
- **アンインストール**: `uninstall.php` は無し。設定（APIキー等）を意図的にDBへ残す方針のため。

## 関連プロジェクト

- **wp_manager (Affiros9)**: Flask + Claude のWordPress記事生成ツール（本体）。このプラグインはその companion。
- **affiros-product-inserter**: `<!--ai-product:...-->` マーカーを実際の商品カードに置換するプラグイン。本プラグインのマーカー挿入機能と対になる。
